import * as React from "react";
import styled from "styled-components";

interface IProps {
  className: string;
  children: React.ReactNode;
}

const Masi = (props: IProps) => {
  return <Wrapper>MasiMasi</Wrapper>;
};

const Wrapper = styled.div``;

export { Masi };
